name: ci-wf
on:
  pull_request:
    branches:
      - develop
    types:
      - opened
      - synchronized
  push:
    branches:
      - develop
jobs:
  linting-job:
    runs-on: ubuntu
     steps:
      - name: welcome steps
        run: echo "CI pipline for this repo"

